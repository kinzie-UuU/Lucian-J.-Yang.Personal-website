# Hero Kinetic Mark Backup

Saved at: 2026-04-25

This folder preserves the current center hero object: the upright glossy black metal kinetic mark inspired by the Cappen reference.

Files:
- `index.current-metal-mark.html`
- `styles.current-metal-mark.css`
- `script.current-metal-mark.js`

Restore:
1. Copy the matching file back to the project root.
2. Rename it to `index.html`, `styles.css`, or `script.js`.

Key hooks in this version:
- HTML: `.hero-kinetic-mark` and `#hero-kinetic-canvas`
- CSS: `.hero-kinetic-mark`, `.hero-kinetic-canvas`
- JS: `initHeroKineticMark(canvas)`

